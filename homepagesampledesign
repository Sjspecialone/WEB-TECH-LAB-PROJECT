<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rebuy - Homepage</title>
    <style>
        /* Basic styling */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        /* Header styling */
        .header {
            background-color: #333;
            color: #fff;
            padding: 1rem;
            text-align: center;
        }

        .title {
            margin: 0;
        }

        .nav a {
            margin: 0 15px;
            color: #fff;
            text-decoration: none;
            transition: color 0.3s;
        }

        .nav a:hover {
            color: #00bcd4;
        }

        /* Animated boxes */
        .content {
            display: flex;
            justify-content: space-around;
            margin: 2rem auto;
            max-width: 1200px;
        }

        .animated-box {
            background-color: #fff;
            border: 1px solid #ccc;
            padding: 1.5rem;
            width: 30%;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.5s, box-shadow 0.5s;
            border-radius: 8px;
        }

        .animated-box:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }

        /* Text animations */
        .text-animation {
            text-align: center;
            margin: 2rem 0;
        }

        .animated-text {
            font-size: 1.5rem;
            opacity: 0;
            animation: fadeInOut 6s linear infinite;
        }

        .animated-text:nth-child(2) {
            animation-delay: 2s;
        }

        .animated-text:nth-child(3) {
            animation-delay: 4s;
        }

        @keyframes fadeInOut {
            0% { opacity: 0; }
            10% { opacity: 1; }
            40% { opacity: 1; }
            50% { opacity: 0; }
            100% { opacity: 0; }
        }
    </style>
</head>
<body>
    <header class="header">
        <h1 class="title">Rebuy</h1>
        <nav class="nav">
            <a href="#features">Features</a>
            <a href="#about">About Us</a>
            <a href="#contact">Contact</a>
        </nav>
    </header>

    <section class="content">
        <div class="animated-box">Feature 1</div>
        <div class="animated-box">Feature 2</div>
        <div class="animated-box">Feature 3</div>
    </section>

    <section class="text-animation">
        <p class="animated-text">Welcome to Rebuy!</p>
        <p class="animated-text">Find the best deals here.</p>
        <p class="animated-text">Shop with ease and confidence.</p>
    </section>
</body>
</html>
